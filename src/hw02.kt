// Вывести на экран пять строк из нулей,
// причем количество нулей в каждой строке равно номеру строки.
fun hw02() {
    var a = "0"
    var b = "0"

    for (i in 1..5) {
        println(a)
        a += b
    }
}
